services:
  - type: web
    name: kurut-ai-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: TOKEN
        sync: false  # Вы установите это в настройках Render
    healthCheckPath: /
    autoDeploy: true
